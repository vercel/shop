---
description: Install and configure the Shopify Headless channel and the apps your storefront needs.
title: Shopify Setup
type: guide
prerequisites:
  - /docs/getting-started
---

All stores require the Shopify [Headless sales channel](https://apps.shopify.com/headless). Install and set it up first. Install the other Shopify apps only when your store needs their storefront features.

## Publish products to Headless

Install Shopify's [Headless sales channel](https://apps.shopify.com/headless). In your Shopify admin, open the **Headless** app under **Sales channels**, then click **Add storefront** to create a storefront.

Grant the storefront's Storefront API token the required permissions:

| Scope                                   | Enables                                            |
| --------------------------------------- | -------------------------------------------------- |
| `unauthenticated_read_product_listings` | Products, collections, search, and recommendations |
| `unauthenticated_read_product_tags`     | Product tags requested by the storefront           |
| `unauthenticated_write_checkouts`       | Cart operations and checkout                       |
| `unauthenticated_read_content`          | Pages, blogs, and articles                         |

For optional scopes and details, see [Storefront API permissions](/docs/reference/storefront-api-permissions).

Publish the products and collections you want to show to the **Headless** channel. See Shopify's guide to [product availability](https://help.shopify.com/en/manual/products/add-update-products#product-availability).

<Callout type="warn" title="Publish to Headless, not only Online Store">
  Content published only to **Online Store** does not appear in Vercel Shop.
</Callout>

To attribute server-side cart activity to this storefront, copy the storefront's numeric ID to `NEXT_PUBLIC_SHOPIFY_STOREFRONT_ID`. Find it in the storefront's URL in the Headless app, after `/headless/`. For example, `https://admin.shopify.com/store/your-store/headless/330461` uses the ID `330461`. Setting it attributes cart and checkout activity to the storefront on the server, which gives you more reliable analytics and funnel metrics that do not depend on the shopper's browser.

## Configure search, filters, and recommendations

Install [Shopify Search & Discovery](https://apps.shopify.com/search-and-discovery) to configure collection and search filters. You can also assign complementary products, which appear as “Pairs Well With” on product pages. Shopify supplies related product recommendations automatically.

## Optional Shopify features and apps

<Callout type="info" title="Enable more with skills">
  Use [skills](/docs/skills) to enable additional Shopify features on your store with a coding
  agent.
</Callout>

### Enable kitting and bundling

Install [Shopify Bundles](https://apps.shopify.com/shopify-bundles) if you sell fixed bundles. Publish the bundle and its component products to **Headless**. Publishing only to **Online Store** is not enough.

## Next steps

<Cards>
  <Card title="Product Setup" href="/docs/shopify/pdp">
    Configure variants, swatches, recommendations, bundles, and product SEO.
  </Card>
  <Card title="Shopify Integration" href="/docs/shopify">
    See how Shopify concepts map to storefront routes and features.
  </Card>
  <Card title="Webhooks" href="/docs/anatomy/webhooks">
    Keep cached storefront data in sync with Shopify Admin changes.
  </Card>
</Cards>
